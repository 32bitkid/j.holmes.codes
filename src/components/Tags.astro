---
interface Props {
  tags: string[];
  class?: string;
}

const { tags, class: className } = Astro.props;
---

<style>
  ul {
    margin: unset;
    padding: unset;
    display: contents;
  }

  ul:empty::after {
    content: 'none';
    font-style: italic;
  }

  li {
    margin: unset;
    display: inline;

    font-family: var(--f-monospace), monospace;
    padding: 0.25em 0.25ch;
    border-radius: 0.3em;
    white-space: normal;
    font-size: 0.85em;
  }
</style>
<ul class:list={[className]}>
  {
    // prettier-ignore
    tags.toSorted().flatMap((tag, idx, arr) => (
      <Fragment key={tag}><li>&num;{tag}</li>{arr.length - 1 !== idx ? ', ' : ''}</Fragment>
    ))
  }
</ul>
